<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="pos_payment_method_view_form_inherit" model="ir.ui.view">
        <field name="name">pos.payment.method.view.form.inherit</field>
        <field name="model">pos.payment.method</field>
        <field name="inherit_id" ref="point_of_sale.pos_payment_method_view_form" />
        <field name="arch" type="xml">

            <field name="split_transactions" position="after">
                <field name="is_Mpesa" invisible="isTillPayment == True" />
                <field name="isTillPayment" invisible="is_Mpesa == True" />
            </field>

        </field>
    </record>

</odoo>