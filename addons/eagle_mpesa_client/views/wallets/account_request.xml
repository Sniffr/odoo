<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Tree (List) View -->
    <record id="view_account_request_list" model="ir.ui.view">
        <field name="name">account.request.list</field>
        <field name="model">account.request</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="requested_account_no"/>
                <field name="company_id"/>
                <field name="currency_id"/>
               
                <field name="state" widget="badge" 
                    decoration-success="state == 'approved'" 
                    decoration-danger="state == 'rejected'" 
                    decoration-info="state == 'new'" 
                    decoration-primary="state == 'sent'"
                />

            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_account_request_form" model="ir.ui.view">
        <field name="name">account.request.form</field>
        <field name="model">account.request</field>
        <field name="arch" type="xml">
            <form string="Account Request" class="oe_form">
                <header>
                    <button name="action_send" type="object" string="Send Request" invisible="state != 'new'" class="btn-primary"/>
                    
                    <field name="state" widget="statusbar"/>
                </header>
                <div class="alert alert-info text-center o_form_header alert-dismissible"
                    invisible="enable_kola_mpesa" role="status">
                    <div>
                        You do not have have Kola M-Pesa enabled, by submitting this request, you will be activating it.
                    </div>
                </div>
                <sheet>
                    <div class="oe_title">
                        <span class="o_form_label">Reference</span>
                        <h1>
                            <field name="name" readonly="1" force_save="1" />
                        </h1>
                    </div>
                    <br />
                    <group>
                        <group>
                            <field name="requested_account_no" readonly="state != 'new'" placeholder="MPL001"/>
                            <field name="paybill_number"/>
                            <field name="enable_kola_mpesa" invisible="1"/>
                        </group>
                        <group>
                            <field name="consent_to_collect_funds"  readonly="state != 'new'"/>
                            <field name="company_id" readonly="1" options="{'no_open':1}" />
                            <field name="reason_for_reject" readonly="1" invisible="state != 'rejected'" decoration-danger="1" />
                        </group>
                    </group>
                </sheet>
                 <chatter />
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_account_request_search" model="ir.ui.view">
        <field name="name">account.request.search</field>
        <field name="model">account.request</field>
        <field name="arch" type="xml">
            <search string="Account Requests">
                <field name="name"/>
                <field name="requested_account_no"/>
                <field name="currency_id"/>
                <field name="state"/>
                <filter string="New" name="new" domain="[('state','=','new')]" />
                <filter string="Sent" name="confirmed" domain="[('state','=','sent')]"/>
                <filter string="Approved" name="approved" domain="[('state','=','approved')]" />
                <filter string="Rejected" name="rejected" domain="[('state','=','rejected')]" />
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_account_request" model="ir.actions.act_window">
        <field name="name">Account Requests</field>
        <field name="res_model">account.request</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first Account Request
            </p>
        </field>
    </record>

   
</odoo>
