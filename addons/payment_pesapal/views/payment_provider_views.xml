<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="payment_provider_form_pesapal" model="ir.ui.view">
        <field name="name">payment.provider.form.pesapal</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='provider_credentials']" position="after">
                <group name="pesapal_credentials" invisible="code != 'pesapal'">
                    <group>
                        <field name="pesapal_consumer_key" password="True"/>
                        <field name="pesapal_consumer_secret" password="True"/>
                    </group>
                    <group>
                        <label for="pesapal_ipn_url"/>
                        <div class="o_row">
                            <field name="pesapal_ipn_url" placeholder="e.g., https://yourdomain.com/payment/pesapal/ipn"/>
                            <button name="action_reset_ipn_url" type="object" string="Reset to Default" class="btn-link" icon="fa-refresh" invisible="code != 'pesapal'"/>
                        </div>
                        <field name="pesapal_api_url" readonly="1"/>
                    </group>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
